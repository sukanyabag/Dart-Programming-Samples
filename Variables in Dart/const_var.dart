// main entry point
// note - everything in dart is an object
main(List<String> arguments){
  
  String ant = 'Ant';
  const String wasp = 'Wasp'; //flagging a var as const, can never change the var val

  print(ant + ' man' + ' and' + ' The ' + wasp);
  
}

